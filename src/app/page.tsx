import TripQuoteForm from "@/components/TripQuoteForm"

export default function Page() {
  return (
    <main>
      <h1>Book & pay inside AI assistants (PoC)</h1>
      <TripQuoteForm />
    </main>
  )
}
